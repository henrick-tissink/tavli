import { ComingSoon } from "@/components/partner/ComingSoon";

export const dynamic = "force-dynamic";

export default function PartnerMenuPage() {
  return (
    <ComingSoon
      title="Menu"
      milestone="M10"
      description="Full menu editor — sections, items, prices, dietary tags, chef's picks, photos, hybrid autosave."
    />
  );
}
